-Створи оголошення функції int add(int a, int b) для юніт-тестування

#ifndef MATH_OPERATIONS_H
#define MATH_OPERATIONS_H

int add(int a, int b);

#endif

-Створи реалізацію функції int add(int a, int b) для юніт-тестування

  #include "math_operations.h"

int add(int a, int b) {
    return a + b;
}

-Створи юніт-тести для функції add з Google Test

#include <gtest/gtest.h>
#include "../math_operations.h"

TEST(MathTest, BasicAddition) {
    EXPECT_EQ(add(2, 3), 5);
}

int main(int argc, char **argv) {
    testing::InitGoogleTest(&argc, argv);
    return RUN_ALL_TESTS();
}

-Додай підтримку Google Test і тестові цілі в CMakeLists.txt

find_package(GTest REQUIRED)
include_directories(${GTEST_INCLUDE_DIRS})
add_executable(runTests tests/unit_tests.cpp math_operations.cpp)
target_link_libraries(runTests ${GTEST_LIBRARIES} pthread)
enable_testing()
add_test(NAME BasicAddition COMMAND runTests)
